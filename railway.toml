# Prefer railway.json for Railway config-as-code.
# If the dashboard still uses Nixpacks, set: Settings → Build → Builder → Dockerfile

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bash scripts/railway-start.sh"
healthcheckPath = "/api"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
